// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyCFZsH0qODGVy2RudRvg3Rsfv7U7vQIxBQ',
    authDomain: 'ng-biblioteca.firebaseapp.com',
    databaseURL: 'https://ng-biblioteca.firebaseio.com',
    projectId: 'ng-biblioteca',
    storageBucket: 'ng-biblioteca.appspot.com',
    messagingSenderId: '260218992223',
    appId: '1:260218992223:web:0cf76f207334dffec88dea'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
